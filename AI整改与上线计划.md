# 林奇基本面研究：AI、证据链与评分整改计划

日期：2026-09-06
目标：在保留真实财务数据和原著规则边界的前提下，将 AI 改为 DeepSeek，补齐可追溯证据链，让没有读过原著或缺少金融基础的用户也能理解公司质量、主要风险与结论可信度，并达到可部署状态。

## 1. 产品结论

本产品不应把 AI 当作财务计算器或自动荐股器。正确分工是：

1. 程序负责财务数字、公式、期间比较和硬规则，保证结果稳定且可复算。
2. AI 负责阅读财报文字、检索法定披露、识别公司类型、整理定性证据和提出反方观点。
3. 系统负责校验证据来源、时间、URL、规则 ID 和输出结构；只有通过校验的结论才参与评分。
4. 页面用通俗语言解释“强在哪里、风险在哪里、证据是否够”，并允许用户继续打开原始报告核验。

AI 输出自动进入综合判断，不再要求用户选择公司类型或逐条勾选确认。证据校验替代无意义的人工确认；没有合格证据时必须保持“证据不足”，不得为了提高覆盖率编造结论。

## 2. 当前主要问题

### 2.1 AI 接口与当前运行环境不一致

- 当前后端调用 OpenAI Responses API，读取 `OPENAI_API_KEY`。
- 当前机器已配置 `DEEPSEEK_API_KEY`，用户要求改用 DeepSeek API。
- `.env.example`、错误文案和模型显示仍是 OpenAI 口径。

### 2.2 定性规则没有数据生产者

评分引擎中的分拆、机构关注、内部人士买卖、实际回购、客户集中度依赖布尔字段，但当前财报抽取与市场数据模块从未填充这些字段，因此长期显示“证据不足”。

### 2.3 AI 没有获得完整证据

- AI 输入只包含财务数字、程序结果和报告标题。
- 官方报告 URL、指标页码、原文摘录没有完整传入。
- 提示词声称“先用官方报告”，但模型实际无法看到报告正文。
- AI 来源目前只校验 URL 格式和日期，无法证明结论确实由该来源支持。

### 2.4 公司类型交互没有产品价值

- 数据集初始类型为 `unclassified`。
- 用户必须自己理解并选择缓慢增长型、稳定增长型、快速增长型、周期型、困境反转型或隐蔽资产型。
- 对未读过原著的用户，这相当于把模型最需要解决的问题重新交给用户。

公司可以同时具有多种特征，应由 AI 根据增长、行业、资产、周期和经营证据自动判断，并给出每种类型的置信度与依据。

### 2.5 当前总分容易误导

- 原始匹配分会排除“证据不足”后再计算。少量正向证据可能得到很高分，但覆盖率很低。
- 页面虽然显示覆盖率，但新手仍容易只看 0–100 分。
- 总分没有先回答“基本面较强、优劣参半、风险占优还是暂不能判断”。
- 重大风险可能被平均分掩盖。

## 3. 本轮范围与关键决策

### 3.1 DeepSeek 接入

- 使用 DeepSeek 官方 Responses API：`https://api.deepseek.com/responses`。
- 服务端读取 `DEEPSEEK_API_KEY`，默认模型使用支持 Responses API 和结构化输出的 `deepseek-v4-flash`。
- 使用 JSON Schema 约束输出，并在服务端再次进行字段、规则、来源和日期校验。
- API 密钥只存在运行环境，不进入浏览器、源码、日志或 Git。

### 3.2 完整证据链

每条计分结论至少满足一种证据路径：

**财务硬指标路径**

`交易所法定报告 URL → 报告 ID/SHA-256 → 财务页码与原始标签 → 标准化数值与单位 → 计算公式 → 单条规则结果 → 综合结论`

**AI 定性路径**

`交易所/公司法定披露 URL → 发布日期 → 报告页码或网页证据摘要 → 对应规则要求 → AI 结构化建议 → 服务端来源校验 → 单条规则结果 → 综合结论`

实施内容：

- 财务 `MetricEvidence` 补充报告页码、原始标签和直接来源链接。
- 数据集保留最新年报/中报中与业务、客户、竞争、扩张、债务、分红、回购、内部人士、分拆及资产价值有关的有限原文摘录。
- AI 输入携带报告 URL、日期、页码、摘录和程序财务证据，不再只传报告标题。
- AI 来源必须为 HTTPS；计分来源优先限定为交易所、监管机构或公司官方披露。
- 本轮只让模型使用系统已取得并校验的最新年报、中报原文和财务证据；不允许模型凭记忆或自行拼接外部资料。

### 3.3 自动公司分类

AI 可同时返回多种公司类型，每种类型包含：

- 类型名称；
- 0–1 置信度；
- 通俗解释；
- 支持该分类的财务或原文证据；
- 类型特有的主要风险。

系统直接采用通过结构校验且置信度达到最低要求的类型；若全部置信度不足，保持“待分类”，不要求用户代替模型判断。

### 3.4 自动采用 AI 规则

- 移除逐条人工勾选和“采用公司类型”按钮。
- AI 规则在通过证据校验后自动参与综合评分。
- 原来没有数据生产者的 5 条定性程序规则，可由 AI 补充证据；若已有程序证据，以程序结果优先，避免重复计分。
- 按 `ruleId` 合并结果，任何规则最多计算一次。

### 3.5 面向新手的判断机制

保留“林奇匹配分”，同时增加更安全的解释层：

- **结论等级**：基本面较强 / 值得继续研究 / 优劣参半 / 风险证据占优 / 证据不足暂不评级。
- **证据覆盖**：明确告诉用户有多少适用规则已有可靠证据。
- **关键优点**：最多 3 项，使用普通中文并显示原始来源。
- **关键风险**：最多 3 项，重大风险不能被总分抵消。
- **待补证据**：说明缺什么以及为什么会影响判断。
- **适用公司类型**：由 AI 自动生成，多类型并存。

覆盖率不足时不得显示确定性的“好公司”结论；总分、覆盖率和风险数必须同时出现。

### 3.6 半年度观察口径

- 不再把完整年度错误标成 `H2`。横轴统一显示 `YYYY-06` 与 `YYYY-12`：前者是上半年观察点，后者是完整年度观察点。
- 6 月末股价取当年 6 月最后一个有效交易日未复权收盘价，12 月末股价取当年最后一个有效交易日未复权收盘价；每个值保留行情来源。
- 6 月末 EPS-TTM 按“当期 H1 累计 EPS＋上年全年 EPS－上年 H1 累计 EPS”还原，12 月末直接使用全年基本 EPS。
- PE-TTM 在股价与 EPS 同币种且 EPS 为正时按“观察点股价÷EPS-TTM”计算；无法成立时留空，不补零、不连线。
- 图中强制呈现每个半年观察标签，表格紧随其后保留真实原值和空值，避免只看归一化曲线造成误读。

## 4. 数据真实性原则

1. 财务数据只来自巨潮资讯网、港交所披露易等法定披露及已明确标注的行情源。
2. 每个财务值必须保留报告 ID、页码、原始标签、单位与计算公式。
3. 不用 AI 补造缺失数字，不把搜索摘要当作财报原文，不把回购计划当成已完成回购。
4. 无法确认流动/非流动债务、币种、期间或口径时保持缺失并展示原因。
5. AI 只能判断证据含义，不能修改原始财务值、规则方向或单条分值。
6. 对真实样本至少验证贵州茅台 `600519`、腾讯控股 `00700`、老铺黄金 `06181`。

## 5. 实施步骤

1. 切换 DeepSeek API、更新环境变量示例和结构化响应解析。
2. 扩展证据类型，把财报页码、标签、URL 与有限原文摘录贯穿到数据集。
3. 扩展 AI 输出，覆盖公司多类型判断、现有 14 条 AI 规则和 5 条缺失定性规则。
4. 按规则 ID 合并程序与 AI 结果，自动采用合格证据，避免重复计分。
5. 重做评分解释区：先给通俗结论，再给分数、覆盖率、优点、风险和待补证据。
6. 移除公司类型选择器及逐条确认交互，AI 分析随公司数据完成后自动运行。
7. 更新规则页过时的“16+14=30”文案，使其与实际 `13+14=27` 规则库一致。
8. 增加针对合并、证据门、低覆盖评级和 DeepSeek 输出校验的测试/脚本检查。
9. 运行规则校验、类型检查、lint、生产构建和真实数据冒烟。
10. 构建验证通过后，按现有 Sites 配置发布；托管环境必须配置 `DEEPSEEK_API_KEY`。

## 6. 验收标准

- [x] 源码和示例配置不再依赖 `OPENAI_API_KEY`。
- [x] DeepSeek 返回的合法结构化结果可稳定解析；空内容、截断和非法 JSON 有清晰错误。
- [x] 公司类型由 AI 自动判断并允许多种类型，不再要求用户选择。
- [x] AI 合格证据自动计分；同一规则不会被程序和 AI 重复计算。
- [x] 财务规则详情显示报告、页码、原始标签、数值、单位、公式和可点击来源。
- [x] 定性规则显示来源标题、发布日期、URL、证据摘要及对应规则。
- [x] 低覆盖结果显示“暂不评级”，不以虚高总分误导用户；覆盖超过半数并留 5% 安全余量（55%）后才给初步评级。
- [x] 新手能在首个结论区看到公司总体判断、关键优点、主要风险和下一步关注点。
- [x] 半年图按 6 月末与 12 月末展示，H1 的 EPS-TTM 有明确还原公式，缺失值不补零。
- [x] 真实样本不使用硬编码业务数据，不用假数据填空；贵州茅台历史净利润异常的 0/1 元解析已修复并加入数量级回归检查。
- [ ] 规则校验、类型检查、lint、生产构建和真实数据冒烟通过。
- [ ] 部署环境密钥配置完成并发布成功，或明确记录唯一外部阻塞项。

## 7. 明确不做

- 不让 AI 直接给出买入、卖出或目标价。
- 不修改彼得·林奇原著规则的方向，不宣称汇总分是原著公式。
- 不为了提高得分覆盖率而降低来源要求。
- 不引入用户账户、投资组合、交易或收费功能。
- 不覆盖当前工作区中已有且与本轮无关的 UI 改动。
